import 'package:flutter/material.dart';

class OrderText extends StatelessWidget {
  const OrderText({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Order Your Food 🔥",
      style: TextStyle(fontSize: 25, fontFamily: "Montaga"),
    );
  }
}
